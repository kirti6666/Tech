import { NextRequest, NextResponse } from "next/server";
import { connectDB } from "@/lib/db";
import { requireAdmin } from "@/lib/middleware/requireAdmin";
import { blogPostSchema } from "@/lib/validations/blog-post";
import BlogPost from "@/models/BlogPost";
export async function PATCH(req: NextRequest, { params }: { params: { id: string } }) { if (!(await requireAdmin(req))) return NextResponse.json({ error: "Forbidden" }, { status: 403 }); try { await connectDB(); const parsed = blogPostSchema.safeParse(await req.json()); if (!parsed.success) return NextResponse.json({ error: parsed.error.issues[0]?.message || "Please check the form" }, { status: 400 }); const existing = await BlogPost.findById(params.id); if (!existing) return NextResponse.json({ error: "Article not found" }, { status: 404 }); const nextSlug = parsed.data.slug || existing.slug; if (await BlogPost.exists({ slug: nextSlug, _id: { $ne: params.id } })) return NextResponse.json({ error: "That URL slug is already in use" }, { status: 409 }); Object.assign(existing, parsed.data, { slug: nextSlug }); await existing.save(); return NextResponse.json({ post: existing }); } catch (error) { console.error("PATCH /api/admin/blog/[id] failed:", error); return NextResponse.json({ error: "Could not save this article" }, { status: 500 }); } }
export async function DELETE(req: NextRequest, { params }: { params: { id: string } }) { if (!(await requireAdmin(req))) return NextResponse.json({ error: "Forbidden" }, { status: 403 }); await connectDB(); const deleted = await BlogPost.findByIdAndDelete(params.id); if (!deleted) return NextResponse.json({ error: "Article not found" }, { status: 404 }); return NextResponse.json({ ok: true }); }
